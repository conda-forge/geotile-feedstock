{% set name = "geotile" %}
{% set version = "1.0.9" %}
package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 0cd0f92f444d7fe7b28815394a15e5a8d9eb9b05fd5de77790ff3a0df697f580

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3.6
    - pip

  run:
    - python >=3.6
    - pip
    - numpy
    - gdal
    - geopandas
    - rasterio

test:
  imports:
    - geotile
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/iamtekson/geotile
  license: MIT
  license_file: LICENSE
  summary: The package for management of geographic raster tiles.
  description: |
    The geotile package is useful for the manipulation of geospatial raster tiles. 
  dev_url: https://github.com/iamtekson/geotile

extra:
  recipe-maintainers:
    - iamtekson
